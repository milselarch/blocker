class Rule {
  constructor ({
    nameSearch,
    nameSearchType,
    programSearch,
    programSearchType,
    blockDuration,
    locked
  }) {
    this.name = name
    if (nameSearchType === undefined) {
      nameSearchType = 
    }
    
    this.nameSearch 
  }
}